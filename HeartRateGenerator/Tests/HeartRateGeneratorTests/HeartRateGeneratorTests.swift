import XCTest
@testable import HeartRateGenerator

final class HeartRateGeneratorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HeartRateGenerator().text, "Hello, World!")
    }
}
